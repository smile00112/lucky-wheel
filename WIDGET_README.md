# Виджет колеса фортуны

Документация по использованию виджета для встраивания колеса фортуны на сторонние сайты.

## Описание

Виджет позволяет встраивать колесо фортуны на любой внешний сайт через простой JavaScript API. 

**Особенности:**
- 🎯 Плавающая иконка в правом нижнем углу страницы
- 🪟 Модальное окно с колесом (открывается по клику на иконку)
- 📱 Адаптивный дизайн (автоматическая адаптация под мобильные устройства)
- 🎨 Современный UI с анимациями
- 🔒 Безопасная коммуникация через postMessage API

Виджет отображается в iframe и взаимодействует с родительской страницей через postMessage API.

## Установка

### 1. Скопируйте файл виджета

Файл виджета находится в `public/js/lucky-wheel-widget.js`. Убедитесь, что этот файл доступен по публичному URL вашего сервера.

### 2. Подключите виджет на целевом сайте

Добавьте следующий код на страницу, где хотите отобразить колесо:

```html
<!-- Контейнер больше не нужен! Виджет сам создаст плавающую иконку -->

<!-- Подключение виджета -->
<script src="https://yourdomain.com/js/lucky-wheel-widget.js"></script>
<script>
  LuckyWheel.init({
    slug: 'wheel-slug',                    // Обязательный: slug колеса
    apiUrl: 'https://yourdomain.com/api/widget',  // Обязательный: URL API
    width: '600px',                        // Ширина модального окна (опционально)
    height: '700px',                       // Высота модального окна (опционально)
    onSpin: function(spinData) {           // Callback при вращении (опционально)
      console.log('Spin:', spinData);
    },
    onWin: function(prize) {               // Callback при выигрыше (опционально)
      console.log('Win:', prize);
      alert('Поздравляем! Вы выиграли: ' + prize.name);
    },
    onError: function(error) {             // Callback при ошибке (опционально)
      console.error('Error:', error);
    },
    onLoad: function() {                   // Callback при загрузке (опционально)
      console.log('Widget loaded');
    }
  });
</script>
```

## Параметры инициализации

### Обязательные параметры

- `slug` (string) - Уникальный идентификатор (slug) колеса, которое нужно отобразить
- `apiUrl` (string) - Базовый URL API виджета (обычно `https://yourdomain.com/api/widget`)

### Опциональные параметры

- `width` (string) - Ширина модального окна. По умолчанию: `600px`
- `height` (string) - Высота модального окна. По умолчанию: `700px`
- **Примечание:** Контейнер больше не нужен! Виджет автоматически создает плавающую иконку в правом нижнем углу страницы.
- `onSpin` (function) - Callback функция, вызываемая при выполнении вращения
- `onWin` (function) - Callback функция, вызываемая при выигрыше приза
- `onError` (function) - Callback функция, вызываемая при возникновении ошибки
- `onLoad` (function) - Callback функция, вызываемая при загрузке виджета

## API методы

### `LuckyWheel.openModal()`

Открыть модальное окно с колесом:

```javascript
LuckyWheel.openModal();
```

### `LuckyWheel.closeModal()`

Закрыть модальное окно:

```javascript
LuckyWheel.closeModal();
```

### `LuckyWheel.spin()`

Программно выполнить вращение колеса (требует открытого модального окна):

```javascript
LuckyWheel.spin();
```

### `LuckyWheel.destroy()`

Уничтожить виджет (удалить иконку, модальное окно и iframe):

```javascript
LuckyWheel.destroy();
```

## События (Callbacks)

### onSpin(spinData)

Вызывается при выполнении вращения.

**Параметры:**
- `spinData` - объект с данными о вращении:
  - `spin_id` - ID вращения
  - `prize` - объект приза (если выигрыш) или `null`
  - `has_prize` - boolean, есть ли приз
  - `spins_count` - текущее количество вращений
  - `spins_limit` - лимит вращений (или `null`)

**Пример:**
```javascript
onSpin: function(spinData) {
  console.log('Вращение #' + spinData.spins_count);
  if (spinData.has_prize) {
    console.log('Выиграли:', spinData.prize.name);
  }
}
```

### onWin(prize)

Вызывается при выигрыше приза.

**Параметры:**
- `prize` - объект приза:
  - `id` - ID приза
  - `name` - название приза
  - `description` - описание приза
  - `text_for_winner` - текст для победителя
  - `type` - тип приза
  - `value` - значение приза

**Пример:**
```javascript
onWin: function(prize) {
  alert('Поздравляем! Вы выиграли: ' + prize.name);
  // Отправить данные на ваш сервер
  fetch('/api/claim-prize', {
    method: 'POST',
    body: JSON.stringify({ prize_id: prize.id })
  });
}
```

### onError(error)

Вызывается при возникновении ошибки.

**Параметры:**
- `error` - объект ошибки или строка с сообщением об ошибке

**Пример:**
```javascript
onError: function(error) {
  console.error('Ошибка виджета:', error);
  // Показать пользователю сообщение об ошибке
}
```

## Примеры использования

### Базовый пример

```html
<!-- Контейнер не нужен! -->
<script src="https://yourdomain.com/js/lucky-wheel-widget.js"></script>
<script>
  LuckyWheel.init({
    slug: 'summer-promo',
    apiUrl: 'https://yourdomain.com/api/widget'
  });
</script>
```

После инициализации в правом нижнем углу страницы появится плавающая иконка. Кликните на неё, чтобы открыть колесо в модальном окне.

### С обработкой событий

```html
<script src="https://yourdomain.com/js/lucky-wheel-widget.js"></script>
<script>
  LuckyWheel.init({
    slug: 'summer-promo',
    apiUrl: 'https://yourdomain.com/api/widget',
    onWin: function(prize) {
      // Показать модальное окно с поздравлением
      showModal('Поздравляем!', 'Вы выиграли: ' + prize.name);
      
      // Отправить данные на ваш сервер
      fetch('/api/track-win', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          prize_id: prize.id,
          prize_name: prize.name
        })
      });
    },
    onError: function(error) {
      console.error('Ошибка:', error);
      alert('Произошла ошибка. Попробуйте позже.');
    }
  });
</script>
```

### Адаптивный виджет

Виджет автоматически адаптируется под размер экрана. На мобильных устройствах модальное окно занимает весь экран.

```html
<script src="https://yourdomain.com/js/lucky-wheel-widget.js"></script>
<script>
  LuckyWheel.init({
    slug: 'summer-promo',
    apiUrl: 'https://yourdomain.com/api/widget',
    width: '600px',
    height: '700px'
  });
</script>
```

## API Endpoints

Виджет использует следующие API endpoints:

- `GET /api/widget/embed/{slug}` - Страница виджета для iframe
- `GET /api/widget/wheel/{slug}` - Получение данных колеса
- `POST /api/widget/guest` - Создание/получение гостя
- `POST /api/widget/spin` - Выполнение вращения
- `GET /api/widget/guest/{guestId}/spins` - История вращений гостя

## Безопасность

- Виджет использует CORS для безопасного взаимодействия с API
- Гости идентифицируются через localStorage
- Все запросы проходят через валидацию на сервере
- Лимиты вращений и призов контролируются на стороне сервера

## Ограничения

- Виджет требует активного интернет-соединения
- Необходимо настроить CORS на сервере для разрешения встраивания
- Виджет работает только с активными колесами (is_active = true)
- Временные ограничения (starts_at, ends_at) проверяются на сервере

## Поддержка браузеров

Виджет поддерживает все современные браузеры:
- Chrome/Edge (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Opera (последние версии)

## Troubleshooting

### Виджет не загружается

1. Проверьте, что файл `lucky-wheel-widget.js` доступен по указанному URL
2. Убедитесь, что CORS настроен правильно на сервере
3. Проверьте консоль браузера на наличие ошибок

### Ошибка "Wheel not found"

1. Проверьте, что slug колеса указан правильно
2. Убедитесь, что колесо активно (is_active = true)
3. Проверьте временные ограничения (starts_at, ends_at)

### Ошибка "Spin limit reached"

Пользователь исчерпал лимит вращений для данного колеса. Это нормальное поведение, если установлен лимит.

### Виджет не отображается в iframe

1. Проверьте настройки X-Frame-Options и Content-Security-Policy
2. Убедитесь, что middleware WidgetCors применен правильно
3. Проверьте настройки безопасности браузера

## Лицензия

Этот виджет является частью приложения LuckyWheel и использует ту же лицензию.

